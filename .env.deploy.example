# QUAD Platform Deployment Credentials
# Copy this file to .env.deploy and fill in real values
# .env.deploy is gitignored and will never be committed

# ============================================
# DEV Environment Credentials
# ============================================
DEV_DB_HOST=postgres-dev
DEV_DB_NAME=quad_dev_db
DEV_DB_PASS=quad_dev_pass

# Google OAuth (DEV)
# Get from: https://console.cloud.google.com/apis/credentials
# OR: vault.nutrinine.app → QUAD Platform → Google OAuth (DEV)
DEV_GOOGLE_CLIENT_ID=your_dev_google_client_id_here
DEV_GOOGLE_CLIENT_SECRET=your_dev_google_client_secret_here

# NextAuth Secret (DEV)
# Generate with: openssl rand -base64 32
DEV_NEXTAUTH_SECRET=your_dev_nextauth_secret_here

# ============================================
# QA Environment Credentials
# ============================================
QA_DB_HOST=postgres-qa
QA_DB_NAME=quad_qa_db
QA_DB_PASS=quad_qa_pass

# Google OAuth (QA)
# TODO: Create separate OAuth client for QA in Google Console
QA_GOOGLE_CLIENT_ID=your_qa_google_client_id_here
QA_GOOGLE_CLIENT_SECRET=your_qa_google_client_secret_here

# NextAuth Secret (QA)
QA_NEXTAUTH_SECRET=your_qa_nextauth_secret_here

# ============================================
# AI (Anthropic Claude API) - Shared across environments
# ============================================
# Get from: https://console.anthropic.com/settings/keys
# OR: vault.nutrinine.app → QUAD Platform → Anthropic API Key
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# ============================================
# Setup Instructions
# ============================================
# 1. Copy this file: cp .env.deploy.example .env.deploy
# 2. Fill in real values (get from vault.nutrinine.app or Google Console)
# 3. Run deployment: ./deploy-studio.sh dev
#
# IMPORTANT: Never commit .env.deploy to git!
